为什么没做 MP3 内嵌歌词功能

Qt 6.11 的 QMediaMetaData 键值列表里没有 Lyrics 这个键。
旧版本（比如 Qt 5）是有的，但 Qt 6 砍掉了。

换句话说：QMediaPlayer 读完 MP3 后，能拿到标题、歌手、专辑，
但拿不到藏在 ID3 标签里的歌词文本（USLT / SYLT 帧）。

想读内嵌歌词就得额外引入 TagLib 之类的第三方库，
但这超出了课程要求的范围（QML + cmake 4.3.4），所以没做。

目前的歌词方案：同目录下的 .lrc / .vtt 外部文件，够用。
